# Отчёт о состоянии проекта

**Дата:** 19 февраля 2026 г.  
**Репозиторий:** `git@github.com:R0m4ik08/cvw-intel.git`  
**Автор проекта:** Бугаев Роман  
**Лицензия:** Apache 2.0

---

## 1. Общее описание проекта

**Цель НИР:** адаптация открытого RISC-V ядра Core-V Wally (5-ступенчатый конвейерный процессор, RV32I) для FPGA-платы Terasic DE2-115 (Intel Cyclone IV E, EP4CE115F29C7). Работа включает:

- Замену Xilinx-специфичных IP-ядер на аналоги Intel
- Адаптацию системы сборки под Quartus Prime
- Разработку моста AHB-Lite <-> Avalon для интерфейсов системных шин
- Создание блоков on-chip памяти (RAM/ROM) для кэша
- Разработку загрузчика (ZSBL) и SDK для пользовательских программ

---

## 4. Архитектура проекта

### 4.1 Структура каталогов (не удачный вариант)

```
intel-cvw/
├── src/               # RTL-исходники ядра Wally (адаптированные под Intel)
│   ├── config/        #   Конфигурация процессора (RV32IMF_Zicsr)
│   ├── cache/         #   Подсистема кэша
│   ├── ebu/           #   External Bus Unit (AHB)
│   ├── fpu/           #   Блок плавающей запятой
│   ├── ieu/           #   Целочисленный исполнительный блок
│   ├── ifu/           #   Блок выборки инструкций + предсказатель переходов
│   ├── lsu/           #   Блок загрузки/хранения
│   ├── mdu/           #   Умножитель/делитель
│   ├── mmu/           #   Блок управления памятью + TLB
│   ├── privileged/    #   Привилегированный режим, CSR
│   ├── uncore/        #   Периферия: UART, GPIO, SPI, CLINT, PLIC
│   └── generic/mem/   #   Обобщённые модули памяти (RAM/ROM)
├── ip_cores/          # Intel-специфичные IP-ядра
│   ├── qsys/src/      #   AHB-to-Avalon мост, SDRAM контроллер
│   ├── ram1p1rwbe/    #   Однопортовая RAM с побайтовой записью
│   ├── ram2p1r1wbe/   #   Двухпортовая RAM (1R+1W)
│   └── rom1p1r_/      #   Однопортовая ROM
├── fpga/src/          # FPGA-уровень: top-модуль, testbench, SRAM модель
├── scripts/           # TCL-скрипты сборки (Quartus, Qsys, Questa, System Console)
├── sdk/               # SDK для пользовательских программ (C/asm)
├── zsbl/              # Zero Stage Boot Loader
├── docker/            # Docker-образ с RISC-V тулчейном
└── constraints/       # Файл ограничений для DE2-115
```

### 4.2 Карта памяти системы (удачный вариант, можно придерживаться)

| Диапазон адресов | Размер | Назначение |
|---|---|---|
| `0x00000000` | — | **AHB MM Start** |
| `0x00000000` — `0x00000FFF` | 15 MB | *UNRESERVED* |
| `0x00001000` | — | RESET_VECTOR  |
| `0x00001000` — `0x00010FFF` | 64 KB | ROM (загрузчик ZSBL) |
| `0x00011000` — `0x00014FFF` | 16 KB | RAM (стек ZSBL) |
| `0x00015000` | — | GPIO |
| `0x00016000` | — | UART |
| `0x00017000` | — | SDC |
| `0x00018000` | — | SPI |
| `0x00019000` — `0x00FFFFFF` | 15 MB | *UNRESERVED* |
| `0x01000000` — `0x0100FFFF` | — | CLINT |
| `0x01010000` — `0x0BFFFFFF` | — | *UNRESERVED* |
| `0x0C000000` — `0x0С3FFFFF` | — | PLIC |
| `0x0C400000` — `0x0FFFFFFF` | 60 MB | *UNRESERVED* |
| `0x10000000` | — | **Avalon MM Start** |
| `0x10000000` — `0x12FFFFFF`| 48 MB | *UNRESERVED* |
| `0x13000000` | — | HEX-дисплей (7-сегментные индикаторы) |
| `0x20000000` — `0x201FFFFF` | 2 MB | SRAM (область пользовательских программ) |
| `0x20200000` — `0x37FFFFFF`| 382 MB | *UNRESERVED* |
| `0x38000000` — `0x3FFFFFFF` | 128 MB | SDRAM (внешняя память) |
| `0x3FFFFFFF` | — | **Avalon MM End** |

### 4.3 Процесс сборки и использование различных инструментов спомощью make

```
make docker_setup        → Docker-образ с RISC-V GCC
make zsbl_build          → Загрузчик (boot.mif)
make qsys_generate       → Генерация Qsys-системы (Platform Designer)
make quartus_create      → Создание проекта Quartus
make quartus_build       → Синтез и сборка FPGA-прошивки
make qsim_create/open    → Симуляция в QuestaSim
```

---

## 5. Ключевые компоненты и их статус

| Компонент | Статус | Комментарий |
|---|---|---|
| **Ядро Wally (RTL)** | Адаптировано | Исходники CVW портированы под Quartus; добавлены `define`-гарды для условного синтеза |
| **Конфигурация (RV32IMF_Zicsr)** | Настроена | RV32I + Multiply + Float + CSR; последнее обновление — 16.02.2026 (FPU) |
| **IP-ядра памяти** | Реализованы | `ram1p1rwbe`, `ram2p1r1wbe`, `rom1p1r_` — Intel MegaFunction |
| **Мост AHB-to-Avalon** | Отлажен (v1.1) | Прошёл через серию исправлений (5 fix-коммитов); статус — рабочий |
| **SRAM/SDRAM контроллеры** | Проверены/работают | `sdram_128mb` — исправлены ошибки 14.02.2026 |
| **SDK** | Реализован | Makefile-система, линкер-скрипт, библиотеки UART/GPIO/CLINT, примеры |
| **ZSBL (загрузчик)** | Реализован | Загрузка из ROM, передача управления в SRAM; MIF-генерация, сборка с помощью SDK |
| **Симуляция (QuestaSim)** | Настроена | Testbench + SRAM модель + waveform-скрипты |
| **Docker-окружение** | Настроено | Ubuntu + RISC-V GCC + elf2hex + Spike + QEMU |
| **System Console (JTAG)** | Реализован | Скрипты загрузки прошивки через JTAG |


